# Browser evidence

This directory contains deterministic Playwright screenshots and later QA records. Images are generated by `npm run test:e2e`; they are evidence of captured pixels, not proof of motion quality or accessibility conformance by themselves.
